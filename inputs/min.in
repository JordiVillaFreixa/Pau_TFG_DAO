Minimize
 &cntrl
  imin=1,
  irest=0,
  maxcyc=2500, ! max number if cycles in the minimization
  ncyc=1000,   ! number of initial steepest descent cycles; the rest until maxcyc will be conjugated gradient
  ntpr=10,
  ntwx=10,
  cut=8.0,
  ntb=1,       ! Use constant volume periodic boundaries (PME is always "on" when NTB > 0)
 /
